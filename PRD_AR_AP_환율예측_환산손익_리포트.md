# PRD: AR/AP 환율 예측 기반 환차·환산손익 리포트 Agent

| 항목 | 내용 |
|---|---|
| 문서 버전 | v1.3 |
| 작성일 | 2026-07-21 (최초) / 2026-08-19 (v1.2, v1.3 개정) |
| 작성자 | 국제금융팀 (환차환산 손익 담당) |
| 상태 | 초안 (Draft) — 확인 필요 항목 있음 |

**v1.2 개정 내역**
- 실제 테스트 원장 파일(`실습AR_300건.xlsx`, `실습AP_300건_v2.xlsx`) 컬럼 구조를 반영해 §4.1 입력데이터 정의를 전면 수정 (적용환율 역산, 필드 매핑, 오픈아이템 필터링 등)
- 환율 전망치 **수동 입력** 기반 즉시 환차·환산손익 계산 기능(F3-2) 신규 추가 — AI 자동수집 시나리오(F3)와 별도로 항상 사용 가능한 경로로 명시

**v1.3 개정 내역** (담당자 UI 검수 피드백 반영)
- §4.2 "당일 스팟환율" → **"당일 매매기준율"**로 용어 변경, 참고용 교차환율(USD/JPY, EUR/USD 등) 자동 계산·표시 요구사항 추가
- 화면 구조를 **1페이지 통합 대시보드에서 4개 페이지(당일 AR/AP 분석 / 환율예측치 / 환차·환산 / 가정사항 및 주의사항)로 분리**하도록 F1~F5 및 §8 재정의
- F3: 시나리오(P10~P90)보다 **기관별 당월말·분기별·내년도 전망치 표를 먼저** 제시하도록 정보 순서 변경
- F3-2: **입력자 필드 폐지**(익명 입력) — 입력 즉시(버튼 없이) 당월 말 기준 손익 미리보기를 표시하고, 별도 버튼으로만 시나리오 비교 목록(최대 5개)에 추가하도록 변경
- F4: 환차손익/환산손익/합계를 하나의 통합 표가 아니라 **완전히 분리된 탭**으로 표시하도록 변경
- 업로드 파일의 컬럼 구조가 SAP 표준과 다를 경우 원 오류(traceback) 대신 **친화적 안내 메시지**(누락 컬럼명 + 필요 컬럼 목록)를 표시하도록 §7 예외 처리 요구사항 추가
- 사이드바 상단 회사 로고 표시 요구사항 추가

---

## 1. 배경 및 목적

국제금융팀은 매일 발생·보유 중인 외화 매출채권(AR)·매입채무(AP)에 대해, 환율 변동이 당월 말 환산/환차 손익에 미치는 영향을 사전에 파악해야 한다. 현재는 담당자가 SAP 원장 데이터를 수기로 내려받아 엑셀로 만기 구조를 분류하고, 환율 전망은 개별적으로 리서치 자료를 검색해 참고하는 방식으로 처리되어 다음과 같은 문제가 있다.

- 매기 반복되는 만기월별 집계·환산 작업에 많은 시간이 소요됨
- 환율 전망치가 담당자 개인의 리서치 접근성에 의존해 일관성이 없음
- "환율이 어떻게 움직이면 손익이 얼마나 되는지"에 대한 시나리오 기반 정량화가 없어 의사결정(헤지 여부 판단 등)에 바로 쓰기 어려움

본 Agent는 **당일 기준 AR/AP 원장 데이터**와 **AI가 수집한 경제기구/기관의 환율 전망치**를 입력받아, 만기 구조·기표환율·예측환율 시나리오별 환산·환차손익을 자동으로 계산해 리포트로 제공한다.
기관의 환율 전망치를 받을 때는 그 출처를 명시한다. 환율전망치는 수동으로 입력할 수 있게 하고, 당일의 AR,AP를 기반으로 당월말 기준으로 수동으로 입력한 환율에 따른 환차손익, 환산손익이 계산될 수 있도록한다. 


## 2. 용어 정의

| 용어 | 정의 |
|---|---|
| AR (Accounts Receivable) | 외화 매출채권. 환율 상승(원화 약세) 시 원화 환산가치 증가 → 이익 방향 |
| AP (Accounts Payable) | 외화 매입채무. 환율 상승(원화 약세) 시 원화 환산가치 증가 → 손실 방향 |
| 전기일 | 채권/채무가 최초 장부에 기표된 날짜 |
| 만기일 | 결제(입금/출금) 예정일 |
| 적용환율(기표환율) | 전기일 당시 적용된 원화 환산 환율 (라인 단위) |
| 외화환산손익 | 만기일이 아직 도래하지 않아 미결제 상태인 채권·채무를 보고기준일(당월 말) 환율로 재평가할 때 발생하는 미실현 손익 (K-IFRS 1021 기준) |
| 외환차손익 | 만기일이 보고기준일 이전(또는 당월 내)이라 실제 결제가 이루어졌거나 이루어질 것으로 보는 채권·채무에서 발생하는 실현 손익 |
| 원/달러 프록시 통합 | EUR·JPY·CNY 등 비USD 통화 익스포저를 당일 스팟 교차환율로 USD 환산한 뒤, 전체 AR/AP를 하나의 원/달러 익스포저로 취급하는 본 리포트의 통합 방식 (§6.4 참조) |
| 오픈아이템(미결제 항목) | 보고기준일 현재 아직 정산(입금/출금 처리)되지 않아 원장에 잔액으로 남아있는 라인. AP 원장의 `반제일` 컬럼에 값이 있으면 이미 정산 완료된 것으로 보아 집계에서 제외 |
| 네고(수출환어음 매입) | 수출 AR을 만기 전에 은행에 매각(할인)해 현금화하는 거래. AR 원장의 `네고여부=Y` 라인은 실질적으로 이미 자금 회수가 끝났을 수 있어 일반 미결 AR과 리스크 성격이 다름 (§4.1, §11 참조) |
| 외화단기차입금 | 외화로 조달한 단기 차입금. 매입채무(AP)와 함께 부채성 외화 익스포저를 구성하나, 성격상 무역채무가 아닌 자금조달 계정으로 별도 리스크 관리 대상일 수 있음 (§4.1, §11 참조) |
| 연체 채권/채무 | 만기일이 보고기준일(오늘)보다 이전인데도 원장에 잔액으로 남아있는 라인. 정상적으로는 이미 결제되어 원장에서 빠져야 하나 실무상 지연 등의 사유로 존재할 수 있음 |

## 3. 사용자 및 활용 시나리오

| 페르소나 | 역할 | 리포트 활용 |
|---|---|---|
| 국제금융팀 담당자 (주 사용자) | 리포트 실행, 전제조건 확인, 배포, 수동 환율 시나리오 입력 | 매일 아침 당일 AR/AP 스냅샷으로 리포트 생성, 이상치 검토 후 배포. 필요 시 자체 판단 환율을 직접 입력(F3-2)해 즉시 손익 재계산 |
| 자금팀/재무팀 | 헤지 실행 여부 판단 | 시나리오별 손익 규모를 보고 선물환/통화옵션 헤지 비중 조정 |
| CFO / 자금부서장 | 의사결정권자 | 월말 예상 손익 레인지(Best/Base/Worst) 확인 |
| 회계팀 (참고) | 결산 반영 검토 | 실제 결산 시 환산/환차손익 처리와 예측치 비교 검증 |

## 4. 입력 데이터 (Input)

### 4.1 AR/AP 원장 (당일 기준, SAP 추출)

테스트용 실제 원장 파일 `실습AR_300건.xlsx`(300행), `실습AP_300건_v2.xlsx`(300행)를 기준으로 컬럼 구조를 정의한다. **AR과 AP는 서로 다른 SAP 리포트(예: FBL5N 계열 vs FBL1N 계열)에서 추출되어 원본 컬럼 구성이 상이**하므로, 두 파일을 각각의 원본 스키마로 파싱한 뒤 §4.1.3의 표준 필드로 정규화하는 과정이 반드시 필요하다.

#### 4.1.1 AR 원장 원본 컬럼 (`실습AR_300건.xlsx`)

| 컬럼 | 설명 | 예시 |
|---|---|---|
| 전기일 | 최초 기표일 (YYYY.MM.DD, 점 구분) | 2026.02.17 |
| 만기일 | 결제 예정일 (YYYY.MM.DD) | 2026.05.18 |
| 전표통화 | 거래 통화 | EUR |
| 미수금액(TC) | 외화 원금 (Transaction Currency) | 157,512.98 |
| 미수금액(LC) | 원화 환산금액 (Local Currency, KRW) | 224,806,131 |
| 비즈니스파트너명 | 거래처명 | ASML Holding |
| G/L계정명 | 계정과목 (외상매출금(외화) / 미수금(외화)) | 외상매출금(외화) |
| 네고여부 | 수출환어음 매입(할인) 여부 (Y/N) | N |
| 전표 유형 | 전표 종류 (RV/DR/DG 등) | DR |
| 업 영역·유통경로·제품군·팀파트 등 | 조직/상품 분류 정보 (집계 옵션용, 필수 아님) | — |

- 샘플 기준 통화 분포: USD 171 / JPY 51 / EUR 50 / CNY 28건 — **JPY, CNY도 포함되므로 §6.4 프록시 통합 로직은 EUR·JPY에 국한하지 않고 KRW 외 전 통화에 범용 적용**되어야 함
- 원본에 **"적용환율" 컬럼이 없음** → §4.1.3 파생규칙으로 라인별 역산 필요
- 원본에 오픈아이템(미결제) 여부를 나타내는 컬럼이 없음 → v1에서는 "미수금액"으로 잡혀있는 전 라인을 미결제로 간주

#### 4.1.2 AP 원장 원본 컬럼 (`실습AP_300건_v2.xlsx`)

| 컬럼 | 설명 | 예시 |
|---|---|---|
| 회계연도 | ⚠️ 컬럼명은 "회계연도"이나 실제 값은 날짜(YYYY.MM.DD) — SAP 추출 시 필드명이 잘못 매핑된 것으로 추정. **전기일로 사용** (§11 확인 필요) | 2025.11.07 |
| 만기일 | 결제 예정일 (YYYY.MM.DD) | 2025.12.07 |
| 전표 통화 키 | 거래 통화 | JPY |
| 전표 통화 금액 | 외화 원금 | 527,539 |
| 회사 코드 통화 금액 | 원화 환산금액 (KRW) | 4,686,498 |
| G/L 계정: 내역 | 계정과목 (외상매입금(외화) / 미지급금(외화) / **외화단기차입금**) | 외상매입금(외화) |
| 전표 헤더 텍스트 | 거래 내용(적요 성격) — 거래처명 전용 컬럼 없음 | 글로벌 물류비 |
| 반제일 | 정산(결제) 완료일. 값이 있으면 이미 결제된 건 | (비어있음 = 미결제) |
| 문서 유형 | 전표 종류 (KG/RE/KR 등) | KG |

- 샘플 기준 통화 분포: JPY 82 / USD 77 / EUR 74 / CNY 67건, 반제일은 300건 전량 비어있음(=전량 오픈아이템)
- **G/L 계정 혼재 주의**: 외상매입금(외화) 104건 + 미지급금(외화) 100건(=매입채무 성격) 외에 **외화단기차입금 96건(전체의 32%)** 이 포함되어 있음. 차입금은 무역채무와 성격이 다른 자금조달 항목이므로 본 리포트의 AP 익스포저에 포함할지 여부는 §11 확인 필요 항목으로 남긴다(v1 기본값: 포함 — 부채성 외화 익스포저로서 환율 상승 시 손실 방향은 매입채무와 동일하므로 동일 로직 적용)
- 원본에도 **"적용환율" 컬럼이 없음** → §4.1.3 파생규칙으로 라인별 역산 필요

#### 4.1.3 표준 필드 매핑 및 파생 규칙

| 표준 필드 | AR 매핑 | AP 매핑 | 비고 |
|---|---|---|---|
| 구분 | 파일 단위로 "AR" 고정 | 파일 단위로 "AP" 고정 | |
| 전기일 | 전기일 | 회계연도 (실제 날짜값) | AP는 컬럼명과 실제 내용 불일치 — §11 |
| 만기일 | 만기일 | 만기일 | |
| 통화 | 전표통화 | 전표 통화 키 | |
| 외화금액 | 미수금액(TC) | 전표 통화 금액 | |
| 원화금액 | 미수금액(LC) | 회사 코드 통화 금액 | |
| **적용환율(파생)** | 원화금액 ÷ 외화금액 | 원화금액 ÷ 외화금액 | 원본에 환율 컬럼이 없어 **라인 단위로 역산**. JPY도 원/1엔 기준(원/100엔 아님, 원본 데이터가 100엔 단위가 아닌 엔화 원단위로 기표되어 있음을 확인) |
| 거래처 | 비즈니스파트너명 | 전표 헤더 텍스트(적요로만 사용, 거래처 식별 불가) | AP는 거래처 전용 컬럼 부재 — §11 |
| 계정과목 | G/L계정명 | G/L 계정: 내역 | AP는 매입채무/차입금 필터링 기준으로 사용 |
| 특이 플래그 | 네고여부(Y/N) | 반제일(비어있지 않으면 오픈아이템에서 제외) | |

- **수집 방식**: 매 영업일 SAP에서 수기 추출 후 파일 업로드 (v1, AR/AP 각각 별도 파일). ERP 직접 연동(API/RFC)은 로드맵(§10) 항목으로 별도 검토.
- **검증/정제 규칙**:
  1. 외화금액 또는 원화금액이 0/결측인 라인은 적용환율 역산 불가 → 예외 처리 후 격리, 담당자 알림
  2. 날짜는 `YYYY.MM.DD` 포맷으로 파싱하며, 월/일 범위를 벗어나는 값(예: `2026.13.40`) 등 오기입 포맷은 방어 후 격리
  3. 만기일 < 전기일인 라인은 오류로 격리
  4. 파생된 적용환율이 해당 통화의 당일 스팟환율(§4.2) 대비 ±30% 이상 괴리되면 데이터 오류 의심으로 경고 표시(격리하지 않고 리포트에는 플래그만 표기)
  5. **AP 오픈아이템 필터링**: `반제일`에 값이 있는 라인은 이미 정산 완료된 것으로 보아 집계 대상에서 제외
  6. **연체 플래그**: 만기일이 보고기준일(오늘)보다 이전인 라인은 "연체"로 별도 표시하고, §6.3 기준상 환차손익 그룹으로 집계(정상적으로는 이미 결제되어 원장에 없어야 하나 실무상 존재 가능 — 실습 데이터에도 다수 확인됨)
  7. **네고(Y) 라인 처리**: v1 기본값은 일반 AR과 동일하게 집계에 포함하되 리포트에 "네고" 플래그를 별도 표기(이미 은행에 할인 매각되어 실질 익스포저가 사라졌을 가능성 있음 — 최종 정책은 §11 확인 필요)

### 4.2 당일 매매기준율 (v1.3부터 "스팟환율"에서 명칭 변경)

> 화면 라벨은 "매매기준율"로 통일하되, §6 계산식에서 "스팟환율"이라는 표현이 나오면 이 절의 값과 동일한 것을 가리킨다(계산 로직 문서에서는 관용적인 금융 용어를 그대로 사용).

- 원/달러(USD/KRW) 및 **당일 AR/AP 원장에 실제 존재하는 모든 통화**(샘플 기준 EUR, JPY, CNY 확인됨)의 원화 매매기준율. 사내 환율 고시 시스템 또는 한국은행/서울외국환중개 고시환율 API에서 자동 수집. 통화 목록은 하드코딩하지 않고 원장에서 동적으로 추출. 담당자가 화면에서 직접 값을 조정(override)할 수 있어야 한다.
- 용도: ①비USD 통화의 USD 환산(프록시 통합), ②그래프의 만기월별 총액 USD 표시 기준.
- **참고용 교차환율(신규, v1.3)**: 위에서 입력받은 KRW 기준 매매기준율로부터 USD/JPY, EUR/USD 등 통화 간 교차환율을 자동 계산해 함께 표시한다. 이는 별도 입력값이 아니라 KRW 기준 매매기준율의 파생값(예: USD/JPY = USD/KRW ÷ JPY/KRW)이며, 담당자가 직접 입력하지 않는다. 목적은 계산이 아니라 담당자가 매매기준율 입력값이 시장 상황과 맞는지 감각적으로 검증할 수 있게 하는 참고 정보 제공이다.

### 4.3 예측환율 원천 데이터 (AI 자동 수집)

- **수집 대상**: 국내외 경제기구·투자은행·리서치 기관이 공개 발표하는 원/달러 환율 전망치 (예: 한국은행 경제전망보고서, IMF WEO, 국내 증권사 리서치센터(현재환율 전망), 해외 IB 컨센서스 서베이 등 공개 자료 한정)
- **수집 대상 시계열 (v1.3 확장)**: 기관별로 ①당월 말, ②올해 잔여 분기별(1~4분기), ③내년도 전망치까지 함께 수집·표시한다. 다만 **F3 백분위 시나리오(P10~P90) 산출에는 ①당월 말 전망치만 사용**하며, 분기별·내년도 값은 담당자가 전체 흐름(포워드 커브)을 참고하도록 보여주는 정보 목적이다 — 계산에는 관여하지 않는다.
- **수집 주기**: 리포트 실행 시 최신 캐시 사용, 원문 재수집은 주 1회(또는 담당자 수동 트리거) — 전망치는 일 단위로 크게 변하지 않으므로 매일 재수집하지 않음
- **출처 로깅**: 수집한 각 전망치의 기관명·발표일·원문 링크(또는 인용)를 리포트 부록에 기록해 추적 가능성 확보 (금융 리포트의 재현성/감사 요건)

## 5. 기능 요구사항

### F1. 만기월별 AR/AP 그래프 (USD 환산)

> **화면 위치(v1.3)**: 1번 페이지 "당일 AR/AP 분석"

- 당일 기준 AR, AP를 **만기일이 속한 월(YYYY-MM)** 로 그룹핑
- 각 라인의 원화금액을 라인 통화의 당일 매매기준율로 USD 환산 (§6.1 신 산식·§6.4 참조 — 통화별 교차환산이며, 모든 라인을 USD/KRW 매매기준율로 일괄 나누는 방식이 아님에 유의)
- AR 합계 막대, AP 합계 막대를 만기월 축 위에 나란히 표시 (Bar chart, 단위: USD)
- **AR/AP/Net을 담당자가 개별적으로 선택해 보이거나 숨길 수 있어야 한다** (v1.3 — 기존 "Net 오버레이 옵션"에서 AR/AP도 함께 토글 가능하도록 확장)
- 최소 6개월(당월 포함 향후 6개월) 만기 구간을 기본 표시, 이후는 "6개월 이후" 묶음으로 합산. 보고기준일보다 만기일이 이미 지난(연체) 라인은 "과거(연체)" 묶음으로 별도 표시(누락되지 않도록 함)
- 상세 표(만기월 버킷별 금액)를 별도로 펼쳐볼 수 있어야 한다

### F2. AR/AP 기표환율 계산 및 표시 (원/달러 환산)

> **화면 위치(v1.3)**: 1번 페이지 "당일 AR/AP 분석" (F1과 같은 페이지)

- AR, AP 각각에 대해 **금액가중평균 기표환율**을 산출해 원/달러 환율로 표시
- 계산식은 §6.4 참조 (비USD 통화 포함 시 프록시 통합 방식 적용)
- 만기월별 기표환율도 함께 표시(전체 평균 외에, 월별 브레이크다운 테이블 제공) — 담당자가 "어느 만기월의 채권이 특히 불리한 환율에 잡혀있는지" 파악 가능

### F3. 예측환율 수집 및 확률 기반 4개 시나리오 산출

> **화면 위치(v1.3)**: 2번 페이지 "환율예측치" (독립 페이지로 분리)
> **정보 순서(v1.3, 담당자 요청 사항)**: 시나리오를 먼저 보여주지 않는다. **① 기관별 전망치 표(당월말·올해 분기별·내년도) → ② 백분위 시나리오** 순서로 배치한다.

- AI가 §4.3 원천에서 기관별 당월 말·분기별·내년도 원/달러 전망치를 수집해 **기관명·발표일·각 시점별 전망치·출처를 한 표**로 먼저 제시한다
- 그 아래에, 수집된 전망치 표본(당월 말 값 기준) 으로 분포를 구성하고 **백분위수 기반**으로 4개 지점을 산출 (기본값: P10 / P50 / P75 / P90 — §11 확인 필요 항목에서 최종 확정)
- 표본 수가 충분치 않을 경우(예: 5개 미만) 경고 표시 후 최소/중앙/최대 기반 대체 산식 적용
- 산출된 4개 예측환율은 확률 레이블(예: "하위 10%(원화강세)", "중앙값", "상위 75%", "상위 90%(원화약세)")과 함께 표시

### F3-2. 수동 환율 입력 기반 즉시 손익 계산

> **화면 위치(v1.3)**: 3번 페이지 "환차·환산" 상단 (기존에는 사이드바 상시 노출이었으나, 담당자 요청으로 해당 페이지 본문으로 이동)

- 담당자가 당월 말 기준 원/달러 예상환율 값을 **직접 입력**할 수 있는 기능을 제공한다 (배경 §1의 핵심 요구사항)
- 수동 입력값은 F3의 AI 자동수집 시나리오와 **독립적으로 항상 사용 가능**해야 함 — AI 수집이 지연·실패하거나 담당자가 자체 판단(개인 리서치, 사내 헤지위원회 합의 환율 등)으로 시나리오를 테스트하고 싶은 경우를 위한 fallback이자 필수 경로
- **입력값을 바꾸면 버튼 없이 즉시** §6.3 산식을 재사용해 **당일 AR/AP → 당월 말 기준** 환차손익·환산손익 미리보기를 표시한다 (v1.3 — 기존에는 "입력 즉시 계산"의 의미가 F4 매트릭스 반영과 혼재돼 있었으나, 미리보기와 시나리오 비교 목록 추가를 분리함)
- 담당자가 별도 버튼을 눌러야 그 값이 F4 시나리오 비교 목록에 **최대 5개까지** 추가된다 — 미리보기 자체는 목록에 추가하지 않아도 항상 볼 수 있다
- **입력자 필드는 두지 않는다 (v1.3, 담당자 결정)** — 누가 입력했는지는 기록하지 않고 입력시각·입력값만 기록한다. §7 추적성 요구사항도 이에 맞춰 "입력시각/입력값" 기준으로 수정한다
- 각 수동 입력값은 F4 매트릭스에 "수동 입력(입력시각)" 라벨로 AI 시나리오와 구분되게 표시되며, AI 수집 출처(§4.3)와 달리 "출처: 담당자 수동 입력"으로 명시해 혼동을 방지
- 입력 이력(입력시각, 입력값)은 **리포트 실행 세션과 무관하게 영구 저장**되어 추적 가능해야 하며, 이번 리포트에 실제로 반영된 값(세션 범위)과 전체 누적 이력(감사 추적용)을 구분해 보여준다 — 과거 세션의 입력값이 오늘 리포트의 시나리오로 자동 포함되어서는 안 됨

### F4. 예측환율 시나리오별 환차·환산손익 계산 및 표시

> **화면 위치(v1.3)**: 3번 페이지 "환차·환산" (F3-2 바로 아래)

- F3에서 산출된 4개 예측환율과 F3-2의 수동 입력값(있는 경우) 각각에 대해, AR/AP 포트폴리오의 환산손익 및 환차손익을 계산 (§6 산식)
- 만기일 기준으로 각 라인을 아래 두 그룹으로 자동 분류:
  - **환산손익 그룹**: 만기일이 당월 말 이후(미결제 예상) → 미실현 재평가
  - **환차손익 그룹**: 만기일이 당월 내(당월 말 이전, 결제 예상) → 실현 손익
- **환차손익 / 환산손익 / 합계를 하나의 통합 표가 아니라 완전히 분리된 화면(탭)으로 표시한다** (v1.3, 담당자 요청 — 기존에는 세 값이 한 표 안에 섞여 있었음). 각 화면 안에서 (AI 시나리오 + 수동 입력) × (AR, AP, Net) 매트릭스로 표시하고, 각 열에 출처(AI 기관명 또는 "수동 입력")를 명시한다
- Base(중앙값) 시나리오 대비 각 시나리오(수동 입력 포함)의 손익 차이(Delta) 표시 — Base 시나리오 자기 자신의 Delta는 0을 표시하는 대신 "(기준)"으로 표기해 혼동을 줄인다

### F5. 리포트 출력

> **v1.3 변경**: 최초 설계는 "1페이지 요약 대시보드"였으나, 담당자 UI 검수 결과 정보량이 많아 한 화면에 몰아두면 가독성이 떨어진다는 피드백에 따라 **4개 페이지로 분리**했다. 데이터 계산 로직(§6)은 페이지 분리와 무관하게 동일하다 — 화면 배치만 바뀐 것이다.

- **웹 대시보드는 아래 4개 페이지로 구성한다** (사이드바에서 페이지 전환):
  1. **당일 AR/AP 분석**: F1(만기월별 그래프, AR/AP/Net 선택 표시) + F2(가중평균 기표환율) + 데이터 품질 배지(격리/제외/연체/네고/매매기준율 이상) + 라인 단위 원장·격리 라인 상세(펼침)
  2. **환율예측치**: F3 — 기관별 당월말/분기별/내년도 전망치 표 → 백분위 시나리오(P10~P90) 순서
  3. **환차·환산**: F3-2(수동 환율 입력 + 즉시 미리보기) + F4(환차손익/환산손익/합계를 탭으로 분리한 시나리오 매트릭스) + 전체 수동 입력 이력(감사용)
  4. **가정사항 및 주의사항**: §6 계산 가정사항, 현재 버전의 한계(미구현 기능·정책 미확정 항목 고지), Excel/PDF 다운로드
- **사이드바(모든 페이지 공통)**: AR/AP 파일 업로드, 보고기준일, 당일 매매기준율(§4.2) + 참고용 교차환율, 회사 로고(상단 고정 표시)
- 상세 부록: 라인 단위 원장, 예측환율 출처 로그(AI 수집 출처 + 수동 입력 이력 포함), 계산 가정사항 명시 — 위 4개 페이지 중 관련 페이지에 분산 배치
- 출력 포맷: 웹 대시보드(1차), Excel 다운로드(2차, 구현 완료) / PDF 다운로드(정책 미확정, §11 항목5 참고 — 버튼만 두고 비활성화)

## 6. 계산 로직 상세

### 6.1 라인별 USD 환산금액

> **v1.1 수정**: 최초 초안은 통화 구분 없이 아래 (구) 산식을 모든 라인에 적용하도록 되어 있었으나, 2단계 구현 중 이 방식이 §6.2의 기표환율을 대수적으로 "항상 당일 USD/KRW 환율"로 붕괴시켜 지표를 무의미하게 만드는 결함이 발견되어 아래 (신) 산식으로 교체함. 상세 근거는 `src/fx_converter.py` 모듈 docstring 참조.

```
(구, 결함 있음) USD환산금액(라인) = 원화금액(라인) ÷ 당일_USD/KRW_스팟환율   ← 모든 통화에 동일 적용 시 §6.2가 상수로 붕괴

(신, 확정) 통화별 당일 스팟환율(§4.2)을 이용한 교차환산:
  USD 라인       : USD환산금액(라인) = 외화금액(라인)
  EUR/JPY 등 라인 : USD환산금액(라인) = 외화금액(라인) × 당일_해당통화/KRW_스팟환율 ÷ 당일_USD/KRW_스팟환율
```

### 6.2 AR/AP 포트폴리오 가중평균 기표환율 (USD 환산 기준)

```
기표환율(AR, USD환산) = Σ 원화금액(AR 전 라인) ÷ Σ USD환산금액(AR 전 라인)
기표환율(AP, USD환산) = Σ 원화금액(AP 전 라인) ÷ Σ USD환산금액(AP 전 라인)
```
※ 만기월별 breakdown 시 동일 산식을 해당 만기월 라인 집합에 적용

### 6.3 시나리오별 환차·환산손익

각 라인을 만기일 기준으로 분류 후, 예측환율(4종) 각각에 대해:

```
손익(AR 라인) = (예측환율 − 기표환율_라인) × USD환산금액(라인)
손익(AP 라인) = (기표환율_라인 − 예측환율) × USD환산금액(라인)
```
- AR: 환율 상승(원화 약세) → (+) 이익 / AP: 환율 상승 → (−) 손실
- 만기일 ≤ 당월 말 → **환차손익**으로 집계, 만기일 > 당월 말 → **환산손익**으로 집계
- Net 손익 = Σ AR 손익 + Σ AP 손익 (자연헤지 효과 반영)

### 6.4 다통화(EUR/JPY/CNY 등) 처리 — 원/달러 프록시 통합 (확정 방식, v1.1)

- EUR, JPY, CNY 등 비USD 익스포저는 §6.1(신)의 통화별 교차환산 산식으로 USD 익스포저로 변환한 뒤, 전체 AR/AP를 통화 구분 없이 "원/달러 환율에 대한 단일 익스포저"로 취급하여 F3/F3-2의 원/달러 시나리오에 일괄 적용. 실제 테스트 원장(§4.1)에는 USD/EUR/JPY/CNY 4개 통화가 확인되었으며, 로직 자체는 원장에 존재하는 임의의 통화에 범용 적용되도록 설계한다(통화 하드코딩 금지)
- 라인별 기표환율(USD환산 기준) = 원화금액(라인, 기표 시점 실제 값) ÷ USD환산금액(라인, §6.1 신 산식) — USD 라인은 자연히 원래의 적용환율과 정확히 일치함
- **한계(가정사항으로 명시)**: 이 방식은 EUR/USD, JPY/USD, CNY/USD 등 개별 통화 자체의 변동성(교차통화 리스크)을 반영하지 않는 근사치이며(교차환산에 "오늘" 시점 스팟환율만 사용, 각 통화의 역사적 변동은 반영하지 않음), 실제 결산상 통화별 환산손익과는 차이가 발생할 수 있음. 리포트 하단에 고지문 필수 표기.

## 7. 비기능 요구사항

| 구분 | 요구사항 |
|---|---|
| 정확성 | 원장 합계·기표환율 계산은 원 단위 오차 없이 재현 가능해야 함 (감사 대응) |
| 추적성 | 예측환율 산출에 사용된 원천 자료(기관명/발표일/링크)와 수동 입력 이력(**입력시각/입력값**, F3-2 — v1.3부터 입력자는 기록하지 않음)은 세션과 무관하게 영구 보관하고, "이번 리포트 반영값"과 "전체 누적 이력"을 구분해 조회 가능해야 함 |
| 실행 주기 | 담당자 수동 트리거(온디맨드) + 평일 오전 자동 실행(스케줄) 병행 |
| 데이터 보안 | 거래처명 등 민감 정보 포함 — 사내 인증된 사용자만 접근, 외부 전송 시 마스킹 검토 |
| 성능 | 원장 라인 수 최대 수만 건 기준 리포트 생성 30초 이내 |
| 예외 처리 | 원장 데이터 포맷 오류(날짜 오기입 등)는 해당 라인만 격리하고 전체 리포트는 정상 생성. **업로드한 파일의 컬럼 구조 자체가 SAP 표준과 다른 경우(v1.3)** 라인 단위 격리로는 처리할 수 없으므로, 원 시스템 오류(traceback)를 그대로 노출하지 않고 어떤 컬럼이 없는지·필요한 컬럼이 무엇인지 안내하는 화면을 표시하고 리포트 생성을 중단해야 함 |

## 8. 시스템 구성 (개략)

```
[AR 원장 파일] ─┐
[AP 원장 파일] ─┼─▶ [스키마 정규화·필드매핑·적용환율 역산(§4.1.3)] ─▶ [데이터 검증/정규화] ─▶ [만기월 분류 & USD환산 엔진] ─▶ [F1 그래프, F2 기표환율]
[당일 스팟환율] ─┘                                                                                                          │
                                                                                                                            ▼
[경제기구 전망 자료] ─▶ [AI 수집 Agent] ─▶ [백분위 산출 엔진(4-시나리오)] ─┐
[담당자 수동 환율 입력(F3-2)] ─────────────────────────────────────────┼─▶ [F4 손익 계산 엔진] ─▶ [리포트 출력]
```

> **v1.3**: 위 데이터 흐름 자체는 바뀌지 않았고, "리포트 출력" 단계만 §5 F5에 정의된 4개 페이지(당일 AR/AP 분석 / 환율예측치 / 환차·환산 / 가정사항 및 주의사항)로 라우팅된다.

## 9. 성공 지표 (KPI)

- 담당자의 리포트 작성 소요시간: 기존 대비 80% 이상 단축
- 예측환율 원천 자료 최신성: 리포트 생성 시점 기준 7일 이내 발표 자료 반영률 100%
- 결산 후 실제 환산/환차손익과 Base(중앙값) 시나리오 예측치 간 오차율 모니터링(참고 지표로 축적)

## 10. 향후 로드맵 (Out of Scope for v1)

- SAP/ERP 실시간 API 연동 (현재는 파일 업로드 기반)
- EUR/KRW, JPY/KRW 개별 전망치까지 수집해 통화별 정밀 계산 (현재는 원/달러 프록시 통합)
- 헤지 포지션(선물환 등) 반영한 Net 익스포저 계산
- 과거 예측치 vs 실제 결과 백테스트 대시보드
- 업로드 파일의 컬럼명이 SAP 표준과 달라도(예: "전기일" 대신 "등록일") 자동으로 매핑해주는 컬럼 별칭/유연 매핑 기능 (v1.3 현재는 컬럼명이 완전히 일치해야 하며, 다르면 필요한 컬럼 목록을 안내하고 중단)

## 11. 확인 필요 항목 (Open Questions)

| # | 항목 | 비고 |
|---|---|---|
| 1 | 백분위수 4개 지점의 정확한 구간(P10/P50/P75/P90 vs 다른 조합) | 국제금융팀 리스크 정책에 맞춰 최종 확정 필요 |
| 2 | 예측환율 원천으로 인정할 "경제기구"의 화이트리스트 (예: 한국은행만 포함할지, 국내 증권사 리서치도 포함할지) | 신뢰도/공신력 기준 필요 |
| 3 | 환차손익/환산손익 분류 기준일을 "당월 말"로 고정할지, 리포트 실행일 기준 "익월 초"까지 유동적으로 볼지 | 결산 캘린더와 정합성 확인 필요 |
| 4 | SAP 원장 파일의 자동 수집 경로(수동 업로드 지속 여부) | IT/SAP 담당 부서 협의 필요 |
| 5 | 리포트 접근 권한 범위 (거래처명 노출 여부 등 민감정보 처리) | 정보보안팀 협의 필요 |
| 6 | AP 원장의 매입채무 외 "외화단기차입금"(샘플 기준 전체의 32%) 을 본 리포트의 AP 익스포저에 포함할지 여부 | 포함 시 이자 등 별도 리스크 요인이 있어 자금팀과 정책 협의 필요. v1 기본값은 포함 |
| 7 | AR "네고여부=Y" 채권(수출환어음 매입분, 샘플 기준 86/300건)의 포함/제외 정책 | 이미 은행에 할인 매각되어 실질 익스포저가 사라졌을 가능성 — 회계팀 확인 필요. v1 기본값은 포함+플래그 표기 |
| 8 | AP 원장에서 전기일에 해당하는 실제 날짜가 "회계연도"라는 컬럼명 아래 들어있는 것으로 보이는 현상이 데이터 정합성 문제인지, 항상 그런 포맷인지 | IT/SAP 담당 부서에 원본 리포트 레이아웃 확인 필요 |
| 9 | 만기일이 보고기준일 이전인 "연체" 라인의 처리 방침 (원장에 포함되는 것이 정상인지, 결제완료 처리 대상인지) | 실습 데이터 기준 다수 존재 확인됨 — 회계팀/자금팀 확인 필요 |
| 10 | 수동 입력 환율(F3-2) 입력 이력 보존 기간·입력 권한(누구나 입력 가능한지, 승인 필요한지) | **일부 확정(v1.3)**: 최대 개수는 리포트(세션)당 5개, 입력자 기록은 하지 않음(익명)으로 결정됨. 보존 기간·입력 권한(누구나 입력 가능한지)은 여전히 미확정 |
| 11 | 업로드 파일의 컬럼명이 SAP 표준과 다를 때 완전 일치를 요구할지, 유사 컬럼명을 자동 매핑할지 | v1.3 기본값은 완전 일치 요구 + 안내 메시지. §10 로드맵 항목과 연계 |

---
