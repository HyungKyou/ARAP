# AR/AP 환차·환산손익 리포트 Agent — 단계별 개발 프롬프트

PRD(`PRD_AR_AP_환율예측_환산손익_리포트.md` v1.2) 기준. 아래 4개 프롬프트를 **순서대로**, 각각 Claude Code 새 세션에 그대로 붙여넣어 사용한다.

- 실행 위치: 이 PRD와 샘플 파일(`실습AR_300건.xlsx`, `실습AP_300건_v2.xlsx`)이 있는 프로젝트 폴더에서 실행할 것 (세션이 바뀌어도 코드/파일은 디스크에 남아있으므로 이어서 진행 가능)
- 스택: Python + pandas + openpyxl + Streamlit (2단계부터 사용)
- 2~4단계 프롬프트는 이전 단계 산출물이 이미 존재한다고 가정하므로, 각 프롬프트 맨 앞에 "먼저 PRD와 기존 코드를 읽고 시작하라"는 지시가 포함되어 있다.

---

## 1단계 — 기본 뼈대 작성

```
너는 국제금융팀 AR/AP 환율 예측 기반 환차·환산손익 리포트 Agent를 만드는 개발 에이전트다.
시작하기 전에 아래를 먼저 전체 읽어라.

- PRD: `PRD_AR_AP_환율예측_환산손익_리포트.md` (v1.2)
- 테스트 데이터: `실습AR_300건.xlsx`(300행), `실습AP_300건_v2.xlsx`(300행)
  → 실제 SAP 추출 컬럼 구조를 그대로 반영한 파일이니, PRD 설명만 믿지 말고 반드시 파일을 직접 열어
    컬럼을 확인하고 진행해라 (매핑 정리는 PRD §4.1.1~4.1.3 참조).

이번 1단계 목표는 "화면 없이 숫자가 맞는" 계산 엔진과 프로젝트 골격을 만드는 것이다.
UI, 예측환율 수집, 시나리오 매트릭스는 다음 단계에서 다룬다. 이번 단계에서 Streamlit UI는 만들지 마라.

스택: Python (pandas, openpyxl). 이후 단계에서 Streamlit을 얹을 것을 감안해
계산 로직은 UI와 완전히 분리된 순수 함수/모듈로 작성해라.

작업 범위:
1. 프로젝트 구조 설계 (예: src/ingest.py, src/fx_engine.py, src/validate.py, tests/)
2. PRD §4.1.1~4.1.3 기준 AR/AP 원장 로더 구현: 서로 다른 두 원본 스키마를
   표준 필드(구분/전기일/만기일/통화/외화금액/원화금액/적용환율(파생)/거래처/계정과목/특이플래그)로 정규화
   - 적용환율 = 원화금액 ÷ 외화금액 (라인 단위 역산, 원본에 환율 컬럼 없음)
   - 날짜는 `YYYY.MM.DD` 포맷 파싱
   - AP는 "회계연도"라는 이름의 컬럼에 들어있는 실제 날짜값을 전기일로 사용
3. PRD §4.1.3의 검증/정제 규칙 7개를 전부 구현
   (0/결측 금액 격리, 날짜 오기입 격리, 만기<전기 오류 격리, 스팟환율 대비 ±30% 괴리 경고,
    AP 반제일 있는 라인 제외, 연체 플래그, 네고 플래그)
4. PRD §6.1(신 산식) 통화별 USD 환산 로직 구현
   — v1.1에서 "구 산식이 §6.2 기표환율을 상수로 붕괴시키는" 결함이 있었다는 경고가 PRD에 있다.
   같은 결함을 재현하지 않도록 각별히 주의하고, 왜 그런지 짧은 주석으로 남겨라.
5. PRD §6.2 가중평균 기표환율(AR/AP, 전체 및 만기월별) 계산 함수
6. 당일 스팟환율은 이번 단계에서는 하드코딩 딕셔너리(USD/EUR/JPY/CNY)로 스텁 처리하되,
   나중에 실제 API로 교체 가능하도록 인터페이스를 분리해라.
7. 두 샘플 파일에 실제로 돌려서 다음을 콘솔에 출력하는 검증 스크립트를 작성해라:
   (a) 몇 건이 격리/플래그 처리됐는지와 사유별 건수
   (b) AR/AP 전체 가중평균 기표환율
   (c) 통화별 라인 수
8. 수기로 검산 가능한 샘플 라인 2~3개를 골라 적용환율 역산·USD환산 결과를 직접 검증하는
   pytest 단위 테스트를 작성해라.

완료 기준: 검증 스크립트 실행 결과를 보여주고, 어떤 라인이 왜 격리/플래그됐는지
요약 리포트를 텍스트로 출력해라.
```

---

## 2단계 — 주요 기능 추가

```
이 프로젝트에는 이미 1단계에서 만든 데이터 정규화 파이프라인과 계산 엔진(fx_engine 등)이 있다.
먼저 `PRD_AR_AP_환율예측_환산손익_리포트.md` (v1.2)와 기존 코드를 다시 읽고 시작해라.

목표: PRD §5의 F1~F5, F3-2를 기능 단위로 전부 구현한다.
이번 단계에서도 화면 디자인은 최소한("동작하는 화면" 수준)이면 충분하다 — 디자인은 3단계에서 다룬다.
대신 계산/로직은 100% 정확해야 한다.

구현할 기능:
- F1: 만기월별 AR/AP USD 막대그래프 + Net 익스포저 라인 오버레이(옵션),
      당월 포함 향후 6개월 표시 + "6개월 이후" 묶음
- F2: AR/AP 전체 가중평균 기표환율 + 만기월별 브레이크다운 테이블
- F3: 예측환율 시나리오 산출
      실시간 웹 수집은 이번 단계 범위 밖이다. 대신:
      (a) 출처 필드(기관명/발표일/링크)를 가진 예측치 표본 데이터 구조를 정의하고
      (b) mock 데이터 소스 어댑터(하드코딩 또는 JSON 파일, 표본 5~10개)를 만들고
      (c) 백분위(P10/P50/P75/P90) 산출 로직과, 표본<5개일 때 최소/중앙/최대 대체 로직을 구현해라.
      실 수집기로 나중에 교체할 수 있도록 어댑터 인터페이스를 깔끔히 분리해라.
- F3-2: 수동 환율 입력 — 최대 5개까지 원/달러 환율을 직접 입력받아
        즉시 §6.3 산식으로 환차손익/환산손익을 계산해 표시. 입력자/입력시각/입력값 이력을 저장해라.
- F4: (AI 4시나리오 + 수동입력 N개) × (환산손익/환차손익/합계) × (AR/AP/Net) 매트릭스 테이블,
      Base(중앙값) 대비 각 시나리오의 Delta 표시
- F5: 1페이지 대시보드(F1+F2+F4 한 화면), 상세 부록(라인단위 원장 / 예측환율 출처 로그 / 계산 가정사항),
      Excel 다운로드(매트릭스+원장). PDF는 §11 확인필요 항목이므로 버튼만 만들고 TODO로 남겨도 된다.

주의사항:
- §6.3 환차손익/환산손익 분류(만기 ≤ 당월말 → 환차손익, > 당월말 → 환산손익) 정확히 반영
- 1단계에서 만든 플래그(연체/네고/AP반제 제외)가 F1~F4 전체에 일관되게 반영되어야 한다.
  격리·제외된 라인이 그래프/평균/매트릭스 어디에서도 몰래 다시 섞여 들어가지 않게 해라.
- 통화를 하드코딩하지 말고, 원장에 실제 존재하는 통화를 동적으로 처리해라(§6.4).

완료 기준: `streamlit run app.py`로 실행해 두 샘플 파일을 업로드하면 F1~F5, F3-2가
모두 화면에 나타나고, 그 값이 1단계 검증 스크립트 결과와 일치하는지 확인해라.
```

---

## 3단계 — UI 디자인

```
이 프로젝트는 2단계까지 기능이 전부 동작하는 상태다.
이번 단계는 로직/계산 결과를 절대 바꾸지 않고, 화면을 보기 좋고 쓰기 좋게 다듬는 것에만 집중한다.
값이 조금이라도 바뀐다면 회귀 버그이니 즉시 원인을 찾아라.

시작하기 전에 `dataviz` 스킬을 불러와 차트/색상 설계 가이드를 참고해라
(F1 막대그래프, F4 매트릭스의 색상 표현 등에 적용).

디자인 요구사항:
- 레이아웃: PRD §5 F5 그대로 — 화면 상단에 수동 환율 입력(F3-2)을 상시 노출되는
  카드나 사이드바로 배치, 그 아래 F1 그래프 → F2 기표환율 요약 → F4 매트릭스 순서로 1페이지 요약,
  상세 부록(라인단위 원장/출처 로그/가정사항)은 별도 탭이나 expander로 분리
- 색상 규칙: AR/AP는 항상 동일한 두 색으로 구분(예: AR=파랑 계열, AP=주황 계열),
  Net은 별도 색의 라인. 손익 (+)/(−)는 직관적인 색(초록/빨강 등)으로 표시하되
  색만으로 구분하지 말고 부호(+/−)나 아이콘을 함께 표기해 접근성을 확보해라.
- 데이터 품질 플래그(연체/네고/스팟환율 괴리 경고/AP 반제 제외)는 표에서
  눈에 띄는 뱃지나 아이콘으로 표시하고, 클릭/expander 시 "왜 이 라인이 플래그됐는지" 설명해라.
- F4 시나리오 매트릭스에서 AI 시나리오와 수동 입력을 시각적으로 구분해라
  (예: 수동입력 컬럼에 "수동" 라벨이나 별도 아이콘). Base 대비 Delta는 화살표/부호로 강조.
- 숫자 포맷 통일: 원화는 천단위 콤마, USD는 소수점 포함, 환율/퍼센트는 소수 2~4자리로 일관되게.
- 라벨/용어는 PRD §2 용어정의를 그대로 사용해라(사용자가 국제금융팀 실무자이므로
  익숙한 회계 용어를 그대로 쓰는 게 낫다).
- 사내 데스크톱 브라우저 기준이라 모바일 반응형까지는 불필요하지만,
  화면 폭이 좁아져도 표가 깨지지 않게 가로 스크롤 처리는 해라.

완료 기준: 앱을 직접 실행해(가능하면 `run` 스킬 활용) 위 요구사항이 반영됐는지 눈으로 확인하고,
계산 결과값이 2단계와 완전히 동일한지(회귀 없음) 재확인해라.
```

---

## 4단계 — PRD 대비 상세 검토 (버그/누락 점검)

```
지금까지 만든 앱을 PRD 원문과 대조해 빠짐없이 검증해라.
이 단계의 목적은 "고치는 것"보다 먼저 "무엇이 틀렸는지 찾아 보고하는 것"이다.
버그 수정 수준의 변경은 바로 적용해도 되지만, 구조를 바꾸는 대규모 리팩터링은
목록으로만 남기고 사용자 승인 전에는 하지 마라.

검토 절차:
1. PRD 전체(특히 §4~§7, §11)를 다시 읽고 요구사항 체크리스트를 만들어라
   (F1, F2, F3, F3-2, F4, F5 / §6.1~6.4 산식 / §4.1.3 검증규칙 7개 / §7 비기능요구사항)
2. 각 항목을 실제 코드와 화면에서 Pass / Fail / 부분구현으로 표시해라.
3. 두 샘플 파일(`실습AR_300건.xlsx`, `실습AP_300건_v2.xlsx`)로 실제 실행하고
   다음을 손으로 재계산해 대조해라:
   - AR/AP 각각 임의 라인 3개씩의 적용환율 역산값, USD환산금액
   - AR/AP 전체 가중평균 기표환율
   - 임의의 수동 입력 환율 1개에 대한 Net 환차손익/환산손익 합계
4. 아래 알려진 리스크 지점을 집중적으로 점검해라:
   - §6.1 "구 산식으로의 회귀" 버그가 재도입되지 않았는지
     (기표환율이 통화별로 다르게 나오는지 확인 — 전부 동일한 값이면 버그다)
   - JPY/CNY 등 비USD 라인이 §6.4 프록시 통합에서 실제로 해당 통화의 스팟환율을 쓰는지,
     혹시 USD 스팟환율로 잘못 나눠지고 있지는 않은지
   - AP `반제일`이 채워진 라인이 정말 집계에서 빠지는지
     (샘플은 전량 미결제라 검증이 안 되면, 임의로 반제일을 채운 테스트 케이스를 추가해 확인)
   - 연체 라인(만기일 < 오늘)이 환차손익 그룹으로 정확히 분류되는지
   - 네고=Y 라인이 플래그만 되고 임의로 제외되지는 않는지(v1 기본값은 "포함")
   - 예측치 표본이 5개 미만일 때 최소/중앙/최대 대체 로직이 실제로 트리거되는지
   - 0/결측 금액, 날짜 오기입 라인이 앱을 죽이지 않고 격리 후 정상 진행되는지
     (일부러 깨진 값을 넣은 테스트 데이터로 확인)
   - 통화가 하드코딩되어 있지 않은지(예: 원장에 GBP 라인 하나를 임의로 추가해 정상 동작하는지 테스트)
5. 3단계 디자인 변경이 2단계의 계산 결과값을 조금이라도 바꾸지 않았는지 재확인해라
   (같은 입력 → 같은 숫자여야 한다).

산출물:
(a) 요구사항 체크리스트 표(Pass/Fail/부분구현)
(b) 발견된 버그 목록 (재현 방법 + 원인 + 심각도)
(c) 즉시 수정한 항목과 보고만 하고 남겨둔 항목 구분
(d) PRD §11 확인필요 항목 중, 정책이 아직 확정되지 않아 "기본값으로 임시 구현해둔" 부분 재확인 리스트
```
